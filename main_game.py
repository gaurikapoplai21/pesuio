import player
jas = player.Player()

def talk_to_bot():
  pass # to talk to the bot 

while playing:
  pass # the game loop where the player will be interacting with the console
  '''
  .
  .
  .
  '''
  if not keys:
    go_back() # example of decision control statements
    
   
with open('records','a') as record:
  record.write(plas.progress)
